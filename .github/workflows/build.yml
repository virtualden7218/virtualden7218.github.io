name: Build and Deploy
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: gh-pages
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Inject
        env:
          AB299F: ${{ secrets.AB299F }}
          B02176: ${{ secrets.B02176 }}
        run: |
          python3 -c "
          import os, sys, pathlib
          tag_a = os.environ.get('AB299F', '')
          tag_b = os.environ.get('B02176', '')
          fpath = pathlib.Path('index.html')
          orig = fpath.read_text(encoding='utf-8')
          updated = orig
          if tag_a:
              updated = updated.replace('<!-- 908f490544b1c36e7d0cf54fff78833ffebb8d -->', tag_a)
          if tag_b:
              updated = updated.replace('<!-- d2d062c25e14eb424575e7eb2d7a37 -->', tag_b)
          if updated == orig and (tag_a or tag_b):
              sys.exit(1)
          if updated != orig:
              fpath.write_text(updated, encoding='utf-8')
          "
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
